---
import Layout from '../layouts/Layout.astro';
---

<Layout
  title="Omnia360 | Soluciones Tecnológicas con IA"
  showFooter={true}
  footerType="home"
  background="/bg-omnia.png"
>
  <Fragment slot="head">
    <style>
      :root {
        --primary-color: #0077cc;
        --light-bg: rgba(255, 255, 255, 0.95);
        --dark-bg: rgba(0, 0, 0, 0.5);
        --max-width: 1000px;
      }

      .hero {
        padding: 6rem 2rem;
        text-align: center;
        background: linear-gradient(to right, #0a0a23, #202044);
        color: white;
      }

      .btn {
        background: var(--primary-color);
        color: white;
        padding: 0.75rem 1.5rem;
        text-decoration: none;
        border-radius: 8px;
        font-weight: 600;
        display: inline-block;
        margin-top: 1rem;
        transition: background 0.3s;
      }

      .btn:hover {
        background: #005fa3;
      }

      .section {
        padding: 3.5rem 2rem;
        border-radius: 1.2rem;
        max-width: var(--max-width);
        margin: 4rem auto;
        transition: background 0.3s ease-in-out;
      }

      .section-white {
        background: var(--light-bg);
        color: #1a1a1a;
        text-align: center;
        box-shadow: 0 0 30px rgba(0, 0, 0, 0.1);
      }

      .section-blur {
        background-color: var(--dark-bg);
        color: white;
        backdrop-filter: blur(6px);
        background-clip: padding-box;
        box-shadow: 0 0 30px rgba(0, 0, 0, 0.25);
      }

      h1, h2 {
        margin-bottom: 1rem;
        font-weight: 700;
      }

      p {
        font-size: 1.1rem;
        line-height: 1.6;
      }

      ul {
        list-style: none;
        padding-left: 0;
        text-align: left;
      }

      ul li {
        margin-bottom: 1.2rem;
        font-size: 1rem;
        line-height: 1.5;
      }

      strong {
        font-weight: bold;
      }
    </style>
  </Fragment>

  <!-- Sección principal (Hero) -->
  <section class="hero">
    <h1>Omnia360</h1>
    <p>Transformamos el futuro aplicando inteligencia artificial a soluciones reales.</p>
    <a class="btn" href="#servicios">Ver proyectos</a>
  </section>

  <!-- Presentación general -->
  <section class="section section-blur">
    <h2>¿Quiénes somos?</h2>
    <p>
      Omnia360 es una empresa tecnológica chilena dedicada al desarrollo de soluciones basadas en inteligencia artificial. Aplicamos IA para resolver desafíos del mundo real en educación, productividad, seguridad y gestión de datos.
    </p>
  </section>

  <!-- Proyectos desarrollados -->
  <section class="section section-white" id="servicios">
    <h2>Proyectos destacados</h2>
    <ul>
      <li><strong>MindGrow:</strong> Tutor educativo adaptativo con IA para personalizar el aprendizaje y reducir brechas escolares.</li>
      <li><strong>IA360:</strong> Plataforma de asistentes inteligentes para automatizar procesos repetitivos en pymes y corporaciones.</li>
      <li><strong>Omnia Data:</strong> Herramienta de análisis visual de datos con insights automáticos para la toma de decisiones.</li>
      <li><strong>IAPyme:</strong> Asistente virtual inteligente para pequeñas empresas, que ofrece apoyo en ventas, finanzas y atención al cliente mediante IA generativa.</li>
      <li><strong>GuardIA360:</strong> Sistema en desarrollo para comunidades y empresas que detecta situaciones de riesgo mediante análisis de video en tiempo real.</li>
    </ul>
  </section>

  <!-- Valor diferencial -->
  <section class="section section-blur">
    <h2>¿Por qué Omnia360?</h2>
    <p>
      Combinamos tecnología de vanguardia con una visión humanista y ética. Nuestro enfoque modular y adaptable permite a organizaciones de todo tamaño incorporar inteligencia artificial sin perder el control de sus procesos ni depender de soluciones genéricas.
    </p>
  </section>
</Layout>
